function escapeHtml(value){return String(value||'').replace(/[&<>"]/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[ch]))}
function domainStatusClass(item){if(item.type==='self')return 'self';if(item.type==='sale')return 'sale';return 'hold'}
function renderDomainDetail(item){const root=document.querySelector('[data-domain-detail]');if(!root)return;const statusClass=domainStatusClass(item);const tags=(item.category||[]).map(t=>`<span class="tag">${escapeHtml(t)}</span>`).join('');const action=item.forSale?'<a class="btn primary" href="/contact/">联系咨询</a>':'<a class="btn primary" href="/projects/">查看相关项目</a>';root.innerHTML=`<article class="template-card hero-card"><div class="eyebrow">Premium Domains</div><h1 class="domain-detail-title">${escapeHtml(item.domain)}</h1><span class="status ${statusClass}">${escapeHtml(item.status||'展示')}</span><p class="hero-desc">${escapeHtml(item.title)}</p><div class="tag-row">${tags}</div><div class="actions">${action}<a class="btn" href="/projects/premium-domains/">返回全部域名</a></div></article><article class="template-card"><h2>域名介绍</h2><p>${escapeHtml(item.description)}</p></article><article class="template-card"><h2>当前状态</h2><div class="domain-meta"><div class="meta-box"><div class="meta-label">域名</div><div class="meta-value">${escapeHtml(item.domain)}</div></div><div class="meta-box"><div class="meta-label">状态</div><div class="meta-value">${escapeHtml(item.status||'展示')}</div></div></div>${item.currentUse?`<p style="margin-top:10px">当前用途：${escapeHtml(item.currentUse)}</p>`:''}</article><article class="template-card"><h2>适合用途</h2><p>${item.forSale?'这个域名适合品牌官网、项目入口、产品落地页或长期品牌资产配置。具体价格和过户方式可以联系咨询。':'这个域名目前主要用于 Jozl / OpenAA 体系内的项目、入口或品牌保护。'}</p></article>`}
async function initDomainDetail(){const slug=document.body.dataset.domainSlug;const root=document.querySelector('[data-domain-detail]');if(!slug||!root)return;try{const res=await fetch('/data/domains.json',{cache:'no-store'});const list=await res.json();const item=list.find(d=>d.slug===slug);if(!item){root.innerHTML='<article class="template-card"><h1>没有找到这个域名</h1><p>请返回 Premium Domains 查看全部域名。</p><div class="actions"><a class="btn primary" href="/projects/premium-domains/">返回全部域名</a></div></article>';return}renderDomainDetail(item);document.title=item.domain+' · '+item.title+' | Jozl.com'}catch(e){root.innerHTML='<article class="template-card"><h1>域名资料读取失败</h1><p>请稍后刷新页面，或通过联系方式咨询。</p></article>'}}
document.addEventListener('DOMContentLoaded',initDomainDetail);
